<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <RuntimeIdentifier>linux-x64</RuntimeIdentifier>
        <Configurations>Debug;Release</Configurations>
        <Platforms>AnyCPU</Platforms>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>

        <NativeOutputDir>$(BaseIntermediateOutputPath)native</NativeOutputDir>
    </PropertyGroup>

    <!-- Release configuration -->
    <PropertyGroup Condition="'$(Configuration)' == 'Release'">
        <PublishSingleFile>false</PublishSingleFile>
        <SelfContained>true</SelfContained>
        <PublishTrimmed>true</PublishTrimmed>
        <PublishReadyToRun>true</PublishReadyToRun>
        <DebugType>none</DebugType>
    </PropertyGroup>

    <Target Name="BuildNative" BeforeTargets="CoreCompile" Condition="'$(Configuration)' == 'Debug'">
        <MakeDir Directories="$(NativeOutputDir)"/>

        <Exec Command="zig cc -shared -g -DDEBUG -fPIC ./Native/termshim.c -o $(NativeOutputDir)/termshim.so -target x86_64-linux-gnu"/>
    </Target>

    <Target Name="BuildNative" BeforeTargets="CoreCompile" Condition="'$(Configuration)' == 'Release'">
        <MakeDir Directories="$(NativeOutputDir)"/>

        <Exec Command="zig cc -shared -O3 -DNDEBUG -fPIC ./Native/termshim.c -o $(NativeOutputDir)/termshim.so -target x86_64-linux-gnu "/>
    </Target>

    <ItemGroup>
        <ContentWithTargetPath Include="$(NativeOutputDir)/libtermshim.so">
            <CopyToOutputDirectory>Always</CopyToOutputDirectory>
            <TargetPath>libtermshim.so</TargetPath>
        </ContentWithTargetPath>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Zin.Platform.Base\Zin.Platform.Base.csproj"/>
    </ItemGroup>
</Project>
